/**
 * Stub de `server-only`.
 *
 * O pacote real existe para quebrar o build se um Client Component importar
 * código de servidor — proteção que queremos manter em produção. No teste o
 * ambiente já é Node, então o import só atrapalharia a verificação.
 */
export {};
