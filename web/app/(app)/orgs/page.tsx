import MinhasOrgs from "@/components/minhas-orgs";

export const dynamic = "force-dynamic";

/** O que a carteira conectada fundou — a porta para gerenciar cada uma. */
export default function PaginaMinhasOrgs() {
  return <MinhasOrgs />;
}
